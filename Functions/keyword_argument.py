def increment(number, by):
    return number + by


# it allows us to specify the argument by name, making it clear what the argument represents.
print(increment(5, by=5))
# This can improve code readability and make it easier to understand the purpose of each argument.
